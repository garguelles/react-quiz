// import { fromJS } from 'immutable';
// import { makeSelectQuizPageDomain } from '../selectors';

// const selector = makeSelectQuizPageDomain();

describe('makeSelectQuizPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
